apiVersion: v1
kind: Service
metadata:
  name: slack-gateway-svc
  namespace: devops-tools
spec:
  selector:
    app: chatops-slack-gateway
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
